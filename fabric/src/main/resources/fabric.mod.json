{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": ["%authors%"],
  "license": "${license}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "me.cameronwhyte.mods.template.fabric.TemplateFabric"
    ]
  },
  "mixins": [
    "template-common.mixins.json",
    "template.mixins.json"
  ],
  "depends": {
    "fabric": "*",
    "minecraft": ">=${minecraftVersion}"
  }
}